package days04;

/**
 * @author jyoon
 * @date 2024. 7. 4. - 오후 12:31:21
 * @subject
 * @content
 *
 */

public class Ex06 {

	public static void main(String[] args) {

		//1) x는 10보다 크다.  x>10

		// 2) x는 10보다 크고, 20보다 작다.  
		//(x>10 && x<20)

		
		
		// 3) x는 2의 배수이다. 나머지가 있냐 없냐로 접근
		//(x%2 ==0)

		/*
		int x = 4;
		System.out.println(x%2 == 0); // true
		x = 5;
		System.out.println(x%2 == 0); // true
		 */

		
		
		// 4) x는 2의 배수 또는 3의 배수이다.   
		//(x%2 == 0)  ||  (x%3 == 0)
		/*
		int x = 9;
		System.out.println(x%2 == 0  ||  x%3 == 0);
		 */


		
		// 5) x는 2의 배수이지만 6의 배수는 아니다. 
		//x%2 == 0 && x%6 != 0
		

		
		// 6) 한 문자(ch)가 숫자이다.  
		//Character.isDigit()
		// '0' '1' ~ '9'
		// char ch = 'x';
		// ch == '0' || ch = '1' || ch == '2'..... || ch =='9'
		
		// kor 우 80이상 90 미만
		// kor >= 80 && kor < 90
		
		// 48 <= ch  && ch<=57 아스키 코드
		// '0' <= ch  && ch<='9' 아스키 코드
		
		

		// 7) 한 문자가 소문자이다. 
		//Character.isLowerCase()
		// ch == 'a' || ch = 'b' || ch == 'c'..... || ch =='z'
		// 97 <= ch && ch <=122
		// 'a' <=ch && ch <= 'z'
		
		
		
		// 8) 한 문자가 알파벳이다 ( 대문자이거나 또는 소문자  ) 
		//Character.isAlpahbetic()
		// ('A' <= ch && ch <= 'Z') || ('a' <= ch && ch <= 'z')
		char ch = 'x';
		// ------------->
		System.out.println(65<=ch && ch <=122); // 91~96까지 체크 중이므로 정확한 로직이 아니다. 따라서 72번이 옳바르다.
		



	}

}
