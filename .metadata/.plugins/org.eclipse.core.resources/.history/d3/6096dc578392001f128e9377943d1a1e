package days03;

//우리 패키지 외부에 있는 클래스를 가져와서 쓸때는 반드시 어떤 클래스의 무엇이다 라고 임포트로 표시해야한다.
//시스템은 왜 안썼나? 하면 자바.lang 안에 있기 때문에 임포트 추가 안해도 컴파일러가 자동으로 import java.lang.*; 이 이루어 졌기 때문이다.

// import java.io.*; 필요 없는 구문도 추가되지만 가능은 하다

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

//import java.lang.*; 

/**
 * @author jyoon
 * @date 2024. 7. 3. - 오전 10:51:29
 * @subject [S]canner 클래스 == 물건, 객체, 개체
 * @content
 *
 */

public class Ex02 {

	public static void main(String[] args) throws IOException {
		//컨트롤 + 쉬프트 + O
		
		// 이름을 표준입력장치(키보드)로 부터 입력받아서 출력
		// 자바의 모든 입출력은 스트림 개념이다. 종류 2개 문자와 바이트 
		// 키보드 -> System.in  -> 문자변환 -> 문자열 생성 (한 문자 한문자를 문자열로 변환해야 함)
		// 바이트 스트림 System.in 그래서 안쓰고 Scanner 씀
		
		// 암기
		// new InputSt 컨트롤 스페이스 써야 import 생겨서 오류 안뜸
		// 빨간줄 생기면 import 없는 것
		//키보드 입력에는 무조건 (System.in)
		
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		// 이놈 자리였던걸 밑으로 바뀜 String name = "이창익";
		
		// Unhandled exception type IOException
		System.out.print("> 이름을 입력: ");
		String name = br.readLine(); // String
		
		System.out.println(name);

	}

}
