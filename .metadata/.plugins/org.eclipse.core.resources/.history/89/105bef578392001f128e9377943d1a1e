package days05;

import java.util.Iterator;

/**
 * @author jyoon
 * @date 2024. 7. 5. - 오후 4:12:36
 * @subject
 * @content
 *
 */

public class Ex08 {

	public static void main(String[] args) {

		//  ASCII [모두 출력] + for문 사용.
		// 7 비트 - 128문자 
		// 8 비트 - 256문자 (확장 아스키코드)		
		// 0~31 제어문자
		// 32~127 일반문자( 숫자, 대+소문자 등)
		// 128~255 사선... 문자
		/*
		for (int i = 0; i < 256; i++) {
			System.out.printf("%d[%c]\n", i, i);
		}
		 */

		// 9       '\t' tab 
		// 10     '\n' LineFeed(LF)
		// 13     '\r' CarrageReturm(CR)

		// [문제] 한 라인에 10개씩 출력하자.
		for (int i = 0, lineNumber = 1; i < 256; i++) {
			if (i%10 == 0) System.out.printf("%d : ",lineNumber++);
			System.out.printf("[%c]", i);
			if (i%10 == 9) System.out.println();
		} // for
		
	}

}
