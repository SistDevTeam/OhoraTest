package days02;


/**
 * @author jyoon
 * @date 2024. 7. 2. - 오후 12:35:54
 * @subject 자바에서 표준 출력 함수
 * @content println(), print(), printf()
 *
 */

public class Ex03_02 {

	public static void main(String[] args) {
		
		// 이름 변수 선언
		// 키를 저장할 변수 선언

		String name = "전재윤";
		double height = 173.5;
		
		// [출력형식]
		// 이름은 "전재윤"이고, 키는 185.23cm 이다.
		
		/*
		System.out.println("이름은 ");
		System.out.print(name);
		System.out.println("이고, 키는 ");
		System.out.print(height);
		System.out.print("cm이다.");
		*/
		
		// 3 + 5 + 7
		// 덧셈은 연산자 방향이 오른쪽으로 처리해 나간다.
		
		// + 문자열 연결 연산자 = 중간에 숫자가 있어도 문자열로 만들어진다.
		
		
		/*
		System.out.print("이름은 " + name + "이고, 키는 "+ height + "cm이다." );
		*/
		
		// 출력형식이 있는 경우 출력하는 메서드 ; printf() + format(형식)
		// [출력형식]
		// 이름은 "전재윤"이고, 키는 185.23cm이다.
		// printf() 3가지 파악
		// 1) 기능, 2) 매개변수 3) 리턴값 PrintStream
		// System.out.printf(String format, Object...args) 매개변수 사용
		// String name => print( %s ) conversion 컨버젼
		//  double name => print( %f ) .2 = 소수점 2째 자리까지 출력
		
		
		
		/*
		System.out.printf("이름은 \" + name + \"이고, 키는 \"+ height + \"cm이다.",
				name, height);
		// 문자열 속에서 쌍 따옴표를 쓰고자 한다면 앞에 \ 역슬래시라는 이스케이프 문자를 써야한다.
		*/
		
		
		System.out.printf("이름은 \" %s \"이고, 키는 \" %.2f \"cm이다.",
				name, height);
		
		
	}
	
	
	
}

