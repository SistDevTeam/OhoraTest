package days06;

import java.util.Random;

/**
 * @author jyoon
 * @date 2024. 7. 8. - 오후 2:10:57
 * @subject
 * @content 암기
 *
 */


public class Ex04_04 {

	public static void main(String[] args) {
		// Chapter 16. 람다식
		// Chapter 17. 스트림 요소 처리

		/*
		 int [] m = new int[10];
		 for (int i = 0; i < m.length; i++) {
			m[i] = (int)( Math.random()*100 )+1;
		 }
		 */

		// 기존에는 위에꺼로 썼다면 이제는 밑에 처럼 람다로
		//                                    1<=   <101

		/*
		 //아래 한줄 암기
		int [] m = new Random().ints(1, 101).limit(10).toArray();
		 */

		//            IntStream 암기                                 optionalInt
		int max = new Random().ints(1, 101).limit(10).max().getAsInt();
		System.out.println(max);










		/*
		 // 0~100 국어점수 Math.random() // 함수
		 // 1~45 국어점수 Math.random()

		 //java.util.Random 클래스
		 Random rnd = new Random(); // 대문자이므로 클래스
		 // rnd.nextInt(int bound);
		 int kor = rnd.nextInt(101);
		 int lotto = rnd.nextInt(45)+1;
		 // rnd.nextBoolean(); ture, false
		 // rnd.nextInt();
		 //rnd.nextLong();
		 */


	}

}
